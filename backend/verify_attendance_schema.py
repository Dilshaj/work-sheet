import pyodbc
import os
from dotenv import load_dotenv

load_dotenv()

# Match the connection string pattern from existing codebase if known, 
# otherwise use common patterns or check database.py
# Re-reading database.py to be sure
